https://mp.weixin.qq.com/s/umvbQl5Z5dJQz2TfpaO8jQ

# 做支付系统要注意的几个技术决策

 

[#支付系统最重要的]()，不是支付流程，而是这三个架构决策

如果你做过支付系统，一定要小心一个问题，就是：

一个支付渠道出了问题，把整个支付系统的支付渠道都一起拖垮了。

支付系统，稳定性问题，一定是技术需求里的p0级别，必须认真对待的。

下面我把我之前做过的支付系统，总结出几个几个核心架构设计，分享一下。

第一，渠道隔离

系统接了微信、支付宝、抖音、钱包、自由卡几个支付渠道。

一开始也讨论过，要不要全部部署在同一个服务，最后还是拆了。

因为不同支付渠道的稳定性、响应时间、限流策略都不一样。

如果全部部署在一个服务，一个渠道出现大量慢请求，线程很快就会被占满，最后受影响的就是所有支付渠道。

支付渠道独立开后，一个渠道有问题，影响的也只是这个渠道。且你还可以在收银台那里，把出故障的支付渠道关闭掉，引导用户用其他正常的支付渠道。
比如我们以前，微信支付出问题时，我们的收银台，微信支付立刻就置灰了，用户只能选择支付宝等其他渠道去支付。

虽然一个支付渠道一个微服务，服务数量是增加了，但换来的是故障不会在不同渠道之间传播。
对支付系统而言，这个是值得的，因为支付系统一出事，影响实在太大了。

第二，支付网关

有些团队会直接让前端调用编排层。项目初期，这样做没有什么问题。

但随着终端越来越多，微信、App、小程序、H5的请求格式开始出现各种差异。

如果这些逻辑全部放到编排层，时间长了，它既负责支付流程，又负责终端适配，职责会越来越重。

后来我们把这些逻辑全部放到了支付网关。参数校验、终端识别、openId获取、请求格式转换，都由支付网关完成。编排层只负责支付流程本身。

第三，支付回调

多个支付渠道都是异步回调，先后顺序完全不可控。而且大部分支付平台都有重试机制。同一笔回调收到多次，是很正常的事情。

如果两个线程同时判断所有渠道都已经回调完成，就可能重复发送MQ，后面的业务也会重复执行。

当时我们的做法是，用RedisHash保存每个渠道的回调状态，一个支付单对应一个Hash。

收到回调以后，再按支付单号加分布式锁，整个处理过程都在锁里面完成。

这样无论渠道怎么重试，最终都只有一个线程推进支付状态。

这三个架构设计，看起来解决的是不同的问题。

一个解决渠道之间的隔离，一个解决终端差异，一个解决回调一致性。

但目标其实是一样的。就是，不要让一个局部故障，演变成整个支付系统的故障。

很多时候，架构设计的价值并不是体现在业务正常的时候。

真正到了线上某个支付渠道异常、某个平台不断重试回调的时候，才知道当初为什么要多拆一个服务、多加一层网关、多做一道状态控制。

 





---
*Source: [WeChat Article](https://mp.weixin.qq.com/s/umvbQl5Z5dJQz2TfpaO8jQ)*